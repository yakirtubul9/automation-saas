"""LLM-light agents.

Stage 8 implements a deterministic, actions-only WhatsApp client agent.
We keep the logic here so it can be exercised in tests and via the
WhatsApp webhook without requiring UI/login context.
"""
