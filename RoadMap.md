פרויקט — ClinicSaaS (WhatsApp‑first למערכת תורים לבית רופאים)
מסמך זה מאפיין את החזון, עקרונות מוצר, הגדרות, ורודמאפ עבודה.

1) מה המוצר
מערכת SaaS לניהול יומן מרכזי ואוטומציות לבית רופאים (וגם רופא עצמאי), שהיא WhatsApp‑first:
* רוב הפעולות השוטפות מתבצעות בוואטסאפ דרך סוכני LLM (שמתרגמים שיחה ל־Intent ומפעילים APIs מוגדרים).
* בנוסף יש UI (דשבורדים) לתפעול, בקרה, וניהול לפי תפקיד.
מטרת־על
למקסם הכנסה לכל שעת צוות/חדר עם מינימום התעסקות מצד הבעלים, על ידי:
* צמצום No‑show / ביטולים
* מילוי “חורים” ביומן
* הגדלת שימור ו־Recall
* מדידה ברורה של ROI (“כמה כסף נכנס בגלל זה?”)

2) מה אנחנו פותרים
עסקים מבוססי תורים מפסידים כסף בגלל:
* תורים ריקים, ביטולים ברגע האחרון ו־No‑show
* יומן שלא מנוצל עד הסוף וחוסר שיטתיות
* מערכות שלא “מדברות” (יומן/וואטסאפ/CRM/טפסים/סליקה)
המערכת מתמקדת ב־תוצאה עסקית

3) קהל יעד MVP והקשר עסקי
* MVP קהל יעד: בית רופאים (Owner/Staff שמנהלים את החדרים ורופאים המשכירים את החדרים לפי שעה/יום/חודש) + רופא עצמאי.
* ערוץ תקשורת: WhatsApp API בלבד.

4) יומן
יש יומן לכל חדר/רופא שמחזיק את כל השיבוצים כדי למנוע התנגשויות ולנהל תפוסה. מה שרואים/עורכים תלוי תפקיד. 

אין הבדל מוצרי בין “שריון” ל“תור”:
* כל שיבוץ הוא Slot ביומן שתופס חדר/זמן ונחשב לשכירות.
* לכל חדר יש יומן
* לכל רופא יש יומן
* Slot יכול להיות:
    * ללא מטופל (זמן שכור, אין Client)
    * עם מטופל (תור, יש Client + Service)
* “קביעת תור” מול מטופל = שיוך מטופל + שירות ל־Slot קיים.
כשמזמינים תור:
1. מסננים חדרים שמתאימים לדרישות השירות
	2.	מסננים חדרים פנויים בזמן
	3.	אם נשארו כמה:
	•	קודם החדר המועדף על הרופא (על המערכת לוודא מול הלקוח האם תמיד לשריין קודם בחדר המועדף ולזכור זאת לשריונים עתידיים)
	•	אחר כך “הכי פחות מנוצל היום” (איזון עומסים)
	•	או “הכי קרוב לרצף התורים של הרופא”

ניהול חדרים לפי תחום (Critical Feature)
המערכת חייבת להתחשב בהתאמת חדר לתחום/שירות (ציוד/התאמות):
* לדוגמה: 3 חדרים לעיניים/אא״ג, 5 לפסיכולוגים, 2 לפיזיותרפיה.
* כשנוצר/משתנה Slot, החיפוש לזמינות נעשה רק בתוך חדרים מתאימים לתחום.

5) משתמשים והרשאות (RBAC) + פרטיות ברירת מחדל
תפקידים
* Provider = הרופא (השוכר)
* Staff = מזכירות
* Owner = בעל בית הרופאים
עקרון פרטיות (Default)
* Staff ו־Owner: ברירת מחדל ללא חשיפת פרטי מטופלים.
    * רואים תפוסה + שם רופא + חדר + שעה
    * לא רואים פרטי Client, אלא אם הוגדר אחרת בעתיד
* Provider: רואה ומנהל את התורים והמטופלים שלו בלבד.
‏Audit בלתי מתפשר
כל שינוי משמעותי מתועד:
* מי ביקש
* מי אישר
* מה השתנה
* לפני/אחרי
* מתי
(אין מחיקה אמיתית — סטטוסים/Soft‑delete אם חייבים).

6) שני ממשקי שיחה (Agents)
‏Agent 1 — רופא ↔ מטופלים (בוואטסאפ העסקי של הרופא)
תפקיד: 
1. לאפשר למטופלים לקבוע/לבטל/לשנות בשפה חופשית מול הווטספ של הרופא. ובמקרה שהרופא אישר בהרשמה - גם מול הווטספ של המרפאה שבו המטופל יזוהה כ- client של הרופא ה- Provider (במידה ולא קיים מטופל כזה, יש להפנות אותו להכנסה למערכת דרך הרופא שלו)
2. לאפשר לרופא לבצע פעולות כמו שריון Slots ע״י הדשבורד שלו או דרך הווטספ של המרפאה (Agent 2) שבו הרופא מזוהה כ‑Provider ומבצע פעולות לעצמו.

‏Agent 2 — Agent תפעולי לבית הרופאים (Owner/Staff)
תפקיד: ניהול משאבים ותפעול, כולל:
* סגירת חדרים לתקלות/תחזוקה
* ייזום שינויי חדר/שעה עקב אילוץ מרפאה
* שליחת בקשות אישור לרופא כשנדרש (Approval flow)

7) הכנסת לקוח (רופא) חדש למערכת
ערוצי כניסה
1. לקוח שולח הודעה לוואטסאפ המזכירות (למשל מהאתר). אם המספר לא מוכר → מתחיל תהליך רישום.
2. אפשר להוסיף לקוח ידנית בדשבורד המזכירות.
תהליך רישום אוטומטי (עם אישור מזכירות בסוף)
1. הלקוח נדרש לחתום על חוזה/מסמכים
2. הלקוח נדרש לשלוח תעודת זהות
3. כאשר הלקוח אושר — הודעת חיווי/אישור
4. אם חסר מידע — הודעה להשלמת מסמכים
5. עד שלקוח לא אושר — לא יכול “להיכנס”/לצפות/לשריין
הגדרת לקוח:
1. בחירת חדר מועדף
2. האם מנהל את המטופלים שלו דרך המערכת שלנו או לא? במידה ולא - אז נדרש לעדכן את קונפיגורציית המערכת. במידה וכן - רישום דרך המזכירות יתאפשר רק לרופא או גם למטופלים שלו?

8) פעולות יומן לפי תפקיד
‏Provider (רופא)
* רואה ומנהל תורים ומטופלים שלו
* יכול ליצור/להזיז/לבטל במגבלות ה־Slots שלו
* יכול לבקש ״חדר מועדף״ והמערכת תזכור זאת גם לעתיד
* יכול לשריין Slots חוזרים/חד‑פעמיים:
    * “תקבע לי עד אוקטובר שעתיים כל שבוע ביום ראשון 12–14”
* כשמטופל מבקש תור, המערכת:
    1. מחפשת Slots של הרופא שהם פנויים (ללא Client)
    2. רק בחדרים מתאימים לתחום
    3. מציעה 2–3 חלונות
    4. בחירה → סיכום → שיוך Client+Service ל־Slot
‏Staff (מזכירות)
* רואות תפוסה + שם רופא + חדר + שעה (ללא פרטי מטופלים)
* ברירת מחדל: לא משנות תורים של רופאים
* במקרה אילוץ מרפאה (למשל תקלה בחדר):
    * מזכירות יוזמת שינוי (חדר/שעה)
    * המערכת שולחת לרופא בקשת אישור:
        * “יש תקלה במזגן בחדר 2. להעביר את התור ב‑18:00 לחדר 3. מאשר?”
    * לאחר אישור הרופא — ביצוע + Audit
‏Owner (בעל הקליניקה)
* רואה תפוסה/חדרים/שיבוץ לפי רופא (ללא פרטי מטופלים כברירת מחדל)
* פעולות תפעוליות: סגירת חדרים, ניהול זמינות בעת תקלה
* אם נוצר קונפליקט עם Slots קיימים — המערכת מציעה חלופות לרופא/לשיבוץ

9) עקרונות LLM (חובה)
* Actions‑only: ה־LLM לא “ממציא” ולא מחליט. הוא רק ממפה שיחה ל־Intent ומפעיל APIs מוגדרים.
* אישור לפני פעולות משנות מצב: קביעה/שינוי/ביטול תמיד עם סיכום קצר + “לאשר?” לפני ביצוע.
* ציות לחוקי העסק: למשל ביטול אוטומטי רק עד 24 שעות לפני התןור, אחרת בקשה לצוות.
* Fallback ברור: חוסר בהירות/סיכון פרטיות → טופס קצר / בחירה מבוקרת / נציג.
* הגנת פרטיות: זיהוי תוכן רפואי/רגיש וניתוב נכון (“בצ׳אט מטפלים רק בענייני תור”).
* תצפית ולוגים: שמירת intents, פעולות, ותיעוד לשחזור/QA.

10) דשבורדים (3 דשבורדים)
דשבורד Provider
* יומן אישי, תורים, מטופלים (רק שלו)
* פעולות שינוי/ביטול, סטטטוסים
* תזכורות, Recall, היסטוריה
* מדדים עסקיים
דשבורד Staff
* תפוסת חדרים, “חורים”, עומסים
* סגירת חדרים לתקלות/תחזוקה
* תפעול שינויי חדר/שעה עקב אילוצי מרפאה עם אישור רופא
* ניטור חריגים
דשבורד Owner
* תמונת מצב: תפוסה לפי חדר/רופא, מגמות שימוש
* מדדים תפעוליים ועסקיים
* תהליכים אדמיניסטרטיביים
* הפקת חשבוניות/קבלות ודוחות

11) אוטומציות שמייצרות ROI
1. תזכורות + אישור הגעה בוואטסאפ
2. מילוי חורים אוטומטי:
    * כשיש Slot שכור ללא מטופל שמתקרב (למשל 24–72 שעות קדימה) או שהתפנה מביטול
    * המערכת פונה למועמדים רלוונטיים (רשימת המתנה/מטופלים מתאימים) בוואטסאפ העסקי של הרופא
    * “מי שמאשר ראשון זוכה” → שיוך ל־Slot
3. Recall אוטומטי:
    * לכל תחום/שירות מגדירים מרווח מומלץ לביקור הבא
    * אם לא נקבע ביקור עד תאריך יעד — המערכת פונה אוטומטית ומציעה לקבוע

12) רודמאפ עבודה מאוחד (כולל מה שכבר עשינו)
הרודמאפ בנוי כך שה־Agents מגיעים רק אחרישיש APIs יציבים, הרשאות, ובדיקות התנגשויות/Audit.
שלב 0 — תשתית מוצרית (Foundations)
מטרה: בסיס יציב לפני “קסם”.
* מודלי ליבה: Business, Membership/RBAC, Provider, Room, Client, Service, Appointment/Slot, Reminder, Audit
* עקרונות פרטיות: מיסוך מטופלים ל־Staff/Owner כברירת מחדל
* בדיקות התנגשויות ביומן (חדר/זמן)
* Audit בסיסי לשינויים
סטטוס: קיים בסיס עובד בפרויקט.

שלב 1 — Smart Reminder: תזכורת חכמה + אישור/ביטול
מטרה: לצמצם No‑show בלחיצה אחת.
מה כלול:
* יצירת תזכורות אוטומטית בעת יצירת Slot/תור:
    * אם התור ≥ 24h קדימה → תזכורת ב־24h וב־3h לפני.
    * אחרת → תזכורת אחת (“בקרוב”).
* שליחת תזכורות:
    * מצב מוקדם: WhatsApp Mock + לוג ל־DB.
    * מצב אמת: WhatsApp Cloud API + Template מאושר + תיעוד מסירה/כשל.
* קישור ציבורי לאישור/ביטול:
    * אישור → confirmed_by_client.
    * ביטול → אפשרי גם בפחות מ‑24 שעות(לא חוסמים).
סטטוס: ✅ ממומש בקוד.

שלב 1.5 — Availability + Assign Client (API‑first)
מטרה: לאפשר ל־Agent להציע חלונות ולשייך מטופל ל־Slot בלי להמציא.
מה כלול:
* GET /api/availability/
    * מחזיר 2–3 אופציות (Slots פנויים) לפי Provider.
    * (ככל שהמודלים קיימים) תוך התחשבות בהתאמת חדר לתחום.
* POST /api/assign-client/
    * Slot → תור עם מטופל (שיוך Client + Service).
    * בדיקות התנגשויות מחדש.
    * יצירת Reminders בהתאם.
    * Audit.
סטטוס: ✅ ממומש בקוד.

שלב 2 — Slots לרופאים (שריון חוזר/חד‑פעמי) + ניהול חדרים לפי תחום
מטרה: לייצר “שכירות זמן” לרופא ואז להפוך לתורים.
* API ליצירת Slots חוזרים/חד‑פעמיים (יומי/שבועי/חודשי) עם ריינג׳ תאריכים
* התאמת חדר לפי תחום/שירות תמיד
* first‑come‑first‑served + הצעת חלופות כשאין זמינות
* Owner/Staff יכולים לחסום חדרים (override) ולגרום להצעת חלופות
סטטוס: ✅ ממומש בקוד.

שלב 3 — שינויי אילוצי מרפאה עם Approval לרופא
מטרה: לאפשר ל-Staff/Owner לשנות חדר/שעה עקב אילוץ מרפאה, רק אחרי אישור רופא, בלי לפגוע באמון.
מה כלול: יצירת Change Proposal לתור קיים (שינוי חדר ו/או זמן + reason). status: pending/approved/rejected/expired/canceled + expires_at + token סודי. שליחת הודעת WhatsApp לרופא עם סיכום + קישורי אישור/דחייה (כולל מסך סיכום לפני פעולה). ביצוע רק לאחר אישור, ובאישור מבצעים Re-validate בזמן אמת (התנגשויות, RoomBlock, התאמת Specialty, שעות פעילות/גריד זמן). אם נכשל → 409 עם סיבה. Audit מלא: יצירה, החלטה, וביצוע (before/after). ללא מחיקה אמיתית
סטטוס: ✅ ממומש בקוד.

שלב 4 — Waitlist / מילוי חורים אוטומטי
מטרה: לסגור “חורים” ביומן.
* רשימת המתנה לפי שירות/ימים/טווחים
* טריגר: Slot פנוי שמתקרב / ביטול
* מנגנון תפיסה/תחרות: “מי מאשר ראשון זוכה”
* שיוך אוטומטי ל־Slot
סטטוס: ✅ ממומש בקוד.

שלב 5 — Recall אוטומטי
מטרה: להפוך טיפול בודד לסדרה.
* פרוטוקולים לפי שירות (מרווח מומלץ)
* יצירת יעד ל־next visit
* טריגר אם לא נקבע עד תאריך יעד
* הודעת WhatsApp + הצעת חלונות
סטטוס: ✅ ממומש בקוד.

שלב 6 -CI בסיסי (בדיקות אוטומטיות על כל Push)

מטרה: למנוע מצב שגרסה שבורה נכנסת לפרודקשן, ולתת ביטחון שכל שינוי עובר בדיקות מינימום באופן אוטומטי.

מה כלול (Deliverables):
‏	•	GitHub Actions Pipeline שרץ אוטומטית על כל push ועל כל pull request:
	•	התקנת תלויות (Python + requirements)
‏	•	python manage.py check
	•	הרצת טסטים: python manage.py test
	•	(מומלץ) lint/format checks: ruff ו/או black --check
‏	•	Fail Fast: אם משהו נכשל — ה־CI נכשל ומציג לוג ברור ב-GitHub.
‏	•	Branch Protection (מומלץ):
	•	חסימת merge ל-main אם ה־CI נכשל (אם עובדים עם PRs).
	•	שדרוגים עתידיים (לא חובה בשלב זה):
‏	•	Coverage מינימלי + דוח כיסוי
	•	הרצת בדיקות E2E (Playwright) בהמשך, בעיקר אחרי שלב 15 או כשיש UI יציב

תוצר הצלחה: כל שינוי שנדחף לגיט עובר בדיקות מינימום אוטומטיות, ואפשר לזהות שבר לפני פריסה.

סטטוס: ✅ ממומש בקוד.

שלב 7 - UI תפעולי בסיסי

מטרה: להפוך את המערכת לשימושית לתפעול יום-יומי ולתחקור אוטומציות, לפני שממשיכים לדוחות/בילינג/Agents.

גישה: Django Server-Rendered + TailwindCSS + HTMX (ואופציונלית Alpine.js), בלי SPA.

מה כלול (Deliverables):
‏	•	Layout אחיד + ניווט Mobile-first
‏	•	Sidebar/Topbar קבועים, Breadcrumbs, חוויית שימוש טובה בטלפון.
‏	•	Design System בסיסי
	•	רכיבים עקביים: Buttons/Inputs/Selects, Tables, Pagination, Modals, Toasts, מצבי Loading/Error/Empty.
	•	מסכים קריטיים לתפעול (MVP)
	1.	יומן/תורים: תצוגת יום/שבוע, יצירה/עריכה, שינוי סטטוסים (confirmed/canceled/completed/no-show), חיפוש/פילטרים בסיסיים.
	2.	לקוחות: רשימה עם חיפוש, פרופיל לקוח, היסטוריית תורים.
	3.	הגדרות קליניקה: חדרים/רופאים/שעות פעילות/שירותים (מינימום נדרש להפעלה).
	•	מסך תפעול/סטטוסים (קריטי לשלב 6–8)
	•	סטטוס שליחות WhatsApp (נשלח/נכשל + סיבה), ריצות Recall/Waitlist/Weekly jobs, Audit Log בסיסי לצפייה.
	•	איכות מינימלית
	•	3–5 בדיקות E2E למסלולים קריטיים (Login → יצירת תור → אישור/ביטול → צפייה בלוג), נגישות בסיסית.

תוצר הצלחה: אפשר לנהל יומן/לקוחות, להבין למה אוטומציה נכשלה, ולעבוד בלי להיכנס ל־Django Admin לרוב הפעולות

סטטוס: ✅ ממומש בקוד.


שלב 8 — Agent שיחה למטופלים (Conversational Client Agent)
מטרה: חוויית וואטסאפ “אנושית” למטופל מול הרופא.
* intents: book / reschedule / cancel / request‑cancel / info
* שאלות מינימליות (סוג טיפול/משך)
* הצעת 2–3 חלונות (על בסיס availability)
* אישור לפני יצירה/שינוי/ביטול
* Privacy guardrails + fallback
* מדידה: זמן סגירת תור, שיעור הצלחה ללא צוות, שיעור טעויות
תלות: שלבים 1–2 לפחות.
סטטוס: ✅ ממומש בקוד.


שלב 9 — Agent תפעולי לבית הרופאים (Owner/Staff) + RBAC קשיח
מטרה: ניהול תפעולי/תובנות בשיחה חופשית.
* RBAC לפי תפקיד + מיסוך מטופלים כברירת מחדל
* אימות חזק למנהלים (whitelist מספרים + שכבת אימות נוספת לפעולות מסוכנות)
* אישור לפני פעולות הרסניות
* Audit בלתי מתפשר
* יכולות: “תציג לי את היום/מחר/שבוע”, “סגור חדר 2”, “העבר תור לחדר 3 (אישור רופא)”
תלות: שלבים 3 ו־6 מומלצים.

שלב 10 - מדדי הצלחה (KPIs) שנמדוד לאורך הדרך
* ירידה ב־No‑show
* % Slots פנויים שמתמלאים (Fill rate)
* זמן ממוצע לסגירת תור בוואטסאפ
* שיעור טיפול ללא התערבות צוות
* אמינות: כשלי שליחה/התנגשויות/פעולות שנדחו
* ROI: הכנסות משוערות = מחיר שירות × תורים שבוצעו


שלב 11 - UI מוצרי מתקדם  — “פוליש, מהירות וחוויית SaaS מלאה”

מטרה: להביא את המערכת לרמת מוצר “מקצועי ומודרני”, אחרי שהפיצ’רים הקריטיים עובדים בשטח.

מה כלול (Deliverables):
	•	שדרוג UX עמוק
	•	זרימות עבודה מהירות, קיצורי פעולה, מסכי Empty חכמים, הודעות שגיאה/הצלחה עקביות, מניעת טעויות (confirmations/undo).
	•	דשבורדים ותובנות
‏	•	KPIs יומי/שבועי, פילוחים, חיפושים מתקדמים, תצוגות ניהול לצוות/Owner.
	•	ביצועים ואיכות
	•	שיפור מהירות טעינה, caching נקודתי, הרחבת בדיקות E2E, בדיקות נגישות קפדניות יותר.
	•	ספריית רכיבים מסודרת
	•	סט קומפוננטות מלא ומתועד, עקביות עיצובית בכל המערכת.
	•	אפשרות אסטרטגית לעתיד (רק אם יש צורך אמיתי)
	•	אם נדרש UI כבד/מורכב: מיגרציה הדרגתית ל-Next.js כ-Frontend נפרד, כש-Django נשאר API (לא חובה, רק אם HTMX מתחיל להגביל).

תוצר הצלחה: המערכת מרגישה כמו SaaS מודרני “ברמת מוצר”, עם חוויית שימוש מהירה, עקבית, וניתנת להרחבה

שלב 12 — הפקת חשבוניות/קבלות אוטומטית (Billing & Invoicing)
מטרה: להפוך תורים שבוצעו להכנסה מדווחת ומתועדת, בלי עבודה ידנית.
מה כלול:
* הגדרת תמחור שירותים והצמדת שירות לכל תור.
* טריגר להפקה:
    * בעת סיום תור (completed) או בעת סימון תשלום.
* הפקת מסמכים:
    * קבלה / חשבונית / חשבונית מס‑קבלה (לפי צורך).
* שליחה אוטומטית:
    * WhatsApp (קישור למסמך) / Email.
* Audit + לוג סטטוס (נוצר/נשלח/נכשל).
* אינטגרציה עתידית לשירותי חשבוניות בישראל (ספק חיצוני) + מספרי מסמכים רציפים.
סטטוס: בתור.